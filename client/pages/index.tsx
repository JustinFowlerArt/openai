import { AiChat } from '../components/aiChat';

const App = () => (
    <div
        className={`h-screen flex flex-col items-center justify-center'
			}`}
    >
        <AiChat />
    </div>
);

export default App;
